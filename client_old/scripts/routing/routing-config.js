// Uses strict mode in the whole script
'use strict';

(function() {
	angular.module('app.routing').config(config);
	
	/*
	 * Configures the module.
	 */
	function config() {
		// TODO: routing config()
	}
})();
