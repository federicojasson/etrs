// Uses strict mode in the whole script
'use strict';

(function() {
	angular.module('app.routing').service('router', routerService);
	
	/*
	 * TODO: comments
	 */
	function routerService() {
		var service = this;
		
		// TODO: routerService()
	}
})();
