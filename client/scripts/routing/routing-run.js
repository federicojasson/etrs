// Uses strict mode in the whole script
'use strict';

(function() {
	angular.module('app.routing').run(run);
	
	/*
	 * Performs module initialization tasks.
	 */
	function run() {
		// TODO: routing run()
	}
})();
