// Uses strict mode in the whole script
'use strict';

(function() {
	// Module
	var module = angular.module('views');
	
	// Controllers
	module.controller('ErrorViewController', ErrorViewController);
	
	/*
	 * Controller: ErrorViewController.
	 * 
	 * Offers logic functions for the error view.
	 */
	function ErrorViewController() {
		var controller = this;
		
		// TODO
	};
})();
