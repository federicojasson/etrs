<?php

/*
 * This script defines the services offered to all the users.
 */

// Gets the app
$app = \Slim\Slim::getInstance();

// Defines a route group
$app->group(ROUTE_GROUP_ANONYMOUS, function() use ($app) {
	
	
	
});
