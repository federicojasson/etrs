<?php

/*
 * This script defines the services offered only to the researchers.
 */

// Gets the app
$app = \Slim\Slim::getInstance();

// Defines a route group
$app->group(ROUTE_GROUP_RESEARCHER, function() use ($app) {
	
	
	
});
