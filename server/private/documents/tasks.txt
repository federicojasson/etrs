- Test email styles.
- Doubt: remove associations when medication (or other entity) is deleted?
- Check input validation process.
- Automate current date-time use.
- Check when to use @ManyToMany, @OneToOne and @ManyToOne.
- Check if all methods are necessary in the entities.
- Set onDelete and onUpdate (if possible) on every association: http://www.doctrine-project.org/jira/browse/DDC-409 http://www.doctrine-project.org/jira/browse/DDC-1628
- @Version: reset when maximum value is reached.
- Implement HTTP caching (if possible): http://docs.slimframework.com/#HTTP-Caching-Overview
- Implement session hijacking detection method using the IP address.
- Check warnings.
- Check /* and /**.
- Check that all files contain license.
- Test http://codebutler.com/firesheep and attempt session hijacking.
- Documentar database schema.
- Documentar API.
- Doubt: check if the entity is expired before accepting it (session, permissions, etc)?
- Idea: allow to ban users. A "banned" column can be added and the access helper can check for this before authorizing the access.
